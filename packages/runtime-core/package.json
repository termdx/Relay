{
  "name": "@relay/runtime-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Relay Runtime engine — the kernel that owns workspace state.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "execa": "^9.5.0",
    "yaml": "^2.6.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
